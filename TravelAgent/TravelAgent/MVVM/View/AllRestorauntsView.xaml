<UserControl x:Class="TravelAgent.MVVM.View.AllRestorauntsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TravelAgent.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <ScrollViewer.Resources>
                <Style TargetType="ScrollBar" BasedOn="{StaticResource CustomScrollBarTheme}"/>
            </ScrollViewer.Resources>
            <ItemsControl ItemsSource="{Binding AllRestoraunts}"
                          HorizontalAlignment="Center"
                          Margin="10">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderThickness="2"
                                Padding="30"
                                CornerRadius="25"
                                Margin="20"
                                BorderBrush="#777">
                            <Grid>
                                <StackPanel HorizontalAlignment="Stretch"
                                            MaxWidth="300">
                                    <TextBlock Text="{Binding Name}"
                                               HorizontalAlignment="Center"
                                               FontWeight="Bold"
                                               FontSize="40"
                                               Margin="20 0"/>
                                    <TextBlock Text="{Binding LocationId}"
                                               HorizontalAlignment="Center"
                                               Margin="0 5"/>
                                    <TextBlock Text="{Binding Stars}"
                                               HorizontalAlignment="Center"
                                               Margin="0 5"/>
                                    <Image Source="../../../Image/Location/belgrade.jpg"
                                           Margin="0 20"/>

                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>


    </Grid>
</UserControl>
